export const ENGINE_VERSION = "0.0.0";

// Scaffold placeholder; scene graph, room loading, and scripting VM land in follow-up Jobs.
export function createEngine(): { version: string } {
  return { version: ENGINE_VERSION };
}
