# Content authoring guide

This guide will document how to author deme game content — rooms, items,
NPCs, dialogue trees, and Lua scripts — as validated JSON, primarily aimed
at AI content authors.

Placeholder for now: no content schemas beyond the base `entity` scaffold
exist yet (see `packages/content-schema/schemas`). Populated as concrete
entity schemas land in follow-up Jobs in EPIC-223.
